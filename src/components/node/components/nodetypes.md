1. Noduri de Declencșare (Triggers / Sursă)Aceste noduri inițiază fluxul de lucru. Nu au porturi de intrare (Input), dar au porturi de ieșire (Output) pentru datele generate de eveniment.Nume NodFuncționalitate (Strategy)Date Output (Exemplu)Eveniment Intern declanșează când o modificare specifică are loc în datele CRM.Obiectul complet (Person, Company, Deal) care a fost modificat.Webhook PrimitAscultă un apel HTTP extern și pornește fluxul.Body (Payload-ul JSON/XML), Headers și Query Parameters din cererea HTTP.Programat (Scheduler)Rulează la un interval fix (e.g., zilnic, săptămânal).Data și ora curentă de execuție.Formular TrimisSe declanșează la trimiterea unui formular (de pe site, landing page etc.).Câmpurile din formularul trimis.2. Noduri de Acțiune (Actions / Efect)Aceste noduri efectuează o operație sau un apel către un serviciu. Au porturi de intrare (pentru datele necesare) și de ieșire (pentru rezultatul operației).Nume NodFuncționalitate (Strategy)CategorizareCreare/Actualizare ÎnregistrareCrează sau modifică un obiect în Attio (e.g., Create Person, Update Deal Stage).Acțiune CRMTrimitere E-mailCompune și trimite un e-mail utilizând un serviciu integrat (SendGrid, Gmail etc.).Acțiune ComunicațieApel HTTP ExternRealizează un apel către o API externă (POST, GET, PUT).Acțiune IntegrareTrimitere Mesaj ChatPostază un mesaj pe o platformă de chat (Slack, Teams).Acțiune Comunicație3. Noduri de Logică/Control (Control Flow)Aceste noduri direcționează fluxul de execuție pe baza unor condiții.Nume NodFuncționalitate (Strategy)DescriereCondiție (If/Else)Verifică o condiție (ex: Deal Value > 10000). Are două porturi de ieșire: True și False.Decizie (Branching)Așteptare (Delay)Întrerupe execuția pentru o perioadă specificată (e.g., 2 ore) sau până la o anumită dată.TimingBuclă (Loop/Iterator)Iterează o listă de elemente din input și execută un sub-flux pentru fiecare element.IterațieGrupare (Merge)Așteaptă execuția mai multor căi (Inputs multiple) pentru a continua fluxul pe o singură cale (Output).Sincronizare4. Noduri de Transformare a Datelor (Data Transformation)Aceste noduri primesc date, le modifică și returnează datele transformate.Nume NodFuncționalitate (Strategy)Exemplu de OperațieMapare/FormatareModifică structura datelor sau formatează valori (ex: convertește un text în majuscule, formatează o dată)."John Doe" -> "JOHN DOE".Calcul MatematicEfectuează operații aritmetice (Adunare, Scădere, Procentaj) pe valori numerice.A + B * C.JSON/XML ParserExtrage (parsează) câmpuri specifice dintr-un șir de caractere JSON sau XML.Extrage email din {"user": {"email": "..."}}.Agregare ListăConsolidează date dintr-o listă (ex: calculează suma tuturor valorilor unui câmp dintr-o listă de înregistrări).SUM(Deal_Value).